id: test
require:
  - weld_deps

data_pack:
  load: src


output: dist

meta:
  weld_deps:
    enable_weld_merging: False
    clean_load_tag: False
    deps:
      - id: "itemio"
        version: "100.1.0"
        source: "smithed"