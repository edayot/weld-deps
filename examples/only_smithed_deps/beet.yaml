id: test
require:
  - weld_deps

data_pack:
  load: src


meta:
  weld_deps:
    enable_weld_merging: False
    clean_load_tag: False
    deps:
      smithed.crafter.dev:
        version: "0.4.0"
      itemio:
        version: "1.0.0"
