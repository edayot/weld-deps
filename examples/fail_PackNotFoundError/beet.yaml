id: test
require:
  - weld_deps

data_pack:
  load: src


output: dist

meta:
  weld_deps:
    enable_weld_merging: False
    clean_load_tag: False
    deps:
      - id: "blablabla"
        version: "0.0.0"
        source: "smithed"