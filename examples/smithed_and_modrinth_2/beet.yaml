id: test
require:
  - weld_deps

data_pack:
  load: src


meta:
  weld_deps:
    merge_with_weld: false
    deps:
      player_motion: "1.3.1"
      code-of-copper:
        version: "0.3.0"
        source: "modrinth"
