id: test
require:
  - weld_deps

data_pack:
  load: src


meta:
  weld_deps:
    deps:
      - id: player_motion
        version: "1.3.1"
      - id: code-of-copper
        version: "0.3.0"
        source: "modrinth"
