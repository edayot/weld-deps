[tool.poetry]
name = "weld-deps"
version = "0.9.1"
description = ""
authors = ["edayot"]
license = "mit"

[tool.poetry.dependencies]
python = "^3.11"
smithed = ">=0.16.0"
beet=">=0.104.1"
requests = "^2.31.0"
semver = "^3.0.2"
black = "^24.4.2"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
